<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= category ? 'Edit Category' : 'Add Category' %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 20px;
      }
      .message {
        margin-bottom: 10px;
      }
      .user {
        font-weight: bold;
      }
      .date {
        color: gray;
        font-size: 0.9em;
      }
    </style>
  </head>
  <body>
    <h1><%= category ? 'Edit Category' : 'Create New Category' %></h1>

    <form
      action="<%= category ? '/category/' + category.id : '/category' %>"
      method="POST"
    >
      <label for="name">Category Name:</label>
      <input
        type="text"
        name="name"
        id="name"
        required
        value="<%= category ? category.name : '' %>"
      />

      <button type="submit">
        <%= category ? 'Update Category' : 'Create Category' %>
      </button>
    </form>
    <a href="/">Back</a>
  </body>
</html>
